/**
 * TypeScript типы и интерфейсы
 * 
 * Централизованное место для всех типов приложения.
 * Используйте для общих типов, которые используются в нескольких модулях.
 * 
 * @example
 * ```typescript
 * export interface User {
 *   id: string;
 *   name: string;
 *   email: string;
 * }
 * ```
 */

