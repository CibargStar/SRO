# Гайд по тестированию модуля Profiles

## 📋 Содержание

1. [Подготовка к тестированию](#подготовка-к-тестированию)
2. [Тестирование через Swagger UI](#тестирование-через-swagger-ui)
3. [Тестирование через cURL](#тестирование-через-curl)
4. [Тестирование через Postman/Insomnia](#тестирование-через-postmaninsomnia)
5. [Проверка безопасности](#проверка-безопасности)
6. [Проверка изоляции профилей](#проверка-изоляции-профилей)
7. [Типичные сценарии](#типичные-сценарии)

---

## 🚀 Подготовка к тестированию

### 1. Создание миграции БД

Перед тестированием необходимо создать миграцию базы данных:

```bash
cd backend
npx prisma migrate dev --name add_profiles_module
npx prisma generate
```

### 2. Запуск сервера

```bash
cd backend
npm run dev
```

Сервер должен запуститься на `http://localhost:3000`

### 3. Получение токена авторизации

Для тестирования API endpoints требуется авторизация. Используйте ROOT пользователя из `.env`:

```bash
# Пример запроса на вход
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "YourRootPassword123!"
  }'
```

Сохраните `accessToken` из ответа для использования в последующих запросах.

---

## 📖 Тестирование через Swagger UI

### Доступ к Swagger UI

1. Откройте браузер
2. Перейдите по адресу: `http://localhost:3000/api-docs`
3. Найдите секцию **Profiles**

### Авторизация в Swagger

1. Нажмите кнопку **Authorize** (🔒) в правом верхнем углу
2. В поле **Value** введите: `Bearer <ваш_accessToken>`
3. Нажмите **Authorize**
4. Закройте модальное окно

Теперь вы можете тестировать все endpoints через Swagger UI.

---

## 🔧 Тестирование через cURL

### Переменные окружения

Для удобства создайте переменные:

```bash
# Windows PowerShell
$BASE_URL = "http://localhost:3000"
$TOKEN = "ваш_accessToken_здесь"

# Linux/Mac
export BASE_URL="http://localhost:3000"
export TOKEN="ваш_accessToken_здесь"
```

### 1. Создание профиля

```bash
# Windows PowerShell
curl -X POST "$BASE_URL/api/profiles" `
  -H "Content-Type: application/json" `
  -H "Authorization: Bearer $TOKEN" `
  -d '{
    "name": "Профиль для WhatsApp",
    "description": "Профиль для работы с WhatsApp рассылками"
  }'

# Linux/Mac
curl -X POST "$BASE_URL/api/profiles" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "name": "Профиль для WhatsApp",
    "description": "Профиль для работы с WhatsApp рассылками"
  }'
```

**Ожидаемый ответ (201):**
```json
{
  "id": "uuid-профиля",
  "userId": "uuid-пользователя",
  "name": "Профиль для WhatsApp",
  "description": "Профиль для работы с WhatsApp рассылками",
  "profilePath": "./profiles/userId/profileId",
  "status": "STOPPED",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z",
  "lastActiveAt": null
}
```

**Сохраните `id` профиля для следующих запросов!**

### 2. Получение списка профилей

```bash
# Базовый запрос
curl -X GET "$BASE_URL/api/profiles" \
  -H "Authorization: Bearer $TOKEN"

# С пагинацией
curl -X GET "$BASE_URL/api/profiles?page=1&limit=10" \
  -H "Authorization: Bearer $TOKEN"

# С фильтрацией по статусу
curl -X GET "$BASE_URL/api/profiles?status=STOPPED" \
  -H "Authorization: Bearer $TOKEN"

# С сортировкой
curl -X GET "$BASE_URL/api/profiles?sortBy=name&sortOrder=asc" \
  -H "Authorization: Bearer $TOKEN"
```

**Ожидаемый ответ (200):**
```json
{
  "data": [
    {
      "id": "uuid-профиля",
      "userId": "uuid-пользователя",
      "name": "Профиль для WhatsApp",
      "description": "Профиль для работы с WhatsApp рассылками",
      "profilePath": "./profiles/userId/profileId",
      "status": "STOPPED",
      "createdAt": "2024-01-01T00:00:00.000Z",
      "updatedAt": "2024-01-01T00:00:00.000Z",
      "lastActiveAt": null
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 10,
    "total": 1,
    "totalPages": 1,
    "hasNextPage": false,
    "hasPrevPage": false
  }
}
```

### 3. Получение профиля по ID

```bash
# Замените PROFILE_ID на реальный ID профиля
curl -X GET "$BASE_URL/api/profiles/PROFILE_ID" \
  -H "Authorization: Bearer $TOKEN"
```

**Ожидаемый ответ (200):**
```json
{
  "id": "uuid-профиля",
  "userId": "uuid-пользователя",
  "name": "Профиль для WhatsApp",
  "description": "Профиль для работы с WhatsApp рассылками",
  "profilePath": "./profiles/userId/profileId",
  "status": "STOPPED",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z",
  "lastActiveAt": null
}
```

### 4. Обновление профиля

```bash
curl -X PATCH "$BASE_URL/api/profiles/PROFILE_ID" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "name": "Обновленное название",
    "description": "Обновленное описание"
  }'
```

**Ожидаемый ответ (200):**
```json
{
  "id": "uuid-профиля",
  "userId": "uuid-пользователя",
  "name": "Обновленное название",
  "description": "Обновленное описание",
  "profilePath": "./profiles/userId/profileId",
  "status": "STOPPED",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:01:00.000Z",
  "lastActiveAt": null
}
```

### 5. Получение статуса профиля

```bash
curl -X GET "$BASE_URL/api/profiles/PROFILE_ID/status" \
  -H "Authorization: Bearer $TOKEN"
```

**Ожидаемый ответ (200):**
```json
{
  "id": "uuid-профиля",
  "status": "STOPPED",
  "lastActiveAt": null
}
```

### 6. Удаление профиля

```bash
curl -X DELETE "$BASE_URL/api/profiles/PROFILE_ID" \
  -H "Authorization: Bearer $TOKEN"
```

**Ожидаемый ответ (204):** Без тела ответа

---

## 📮 Тестирование через Postman/Insomnia

### Настройка коллекции

1. Создайте новую коллекцию **Profiles API**
2. Добавьте переменные коллекции:
   - `base_url`: `http://localhost:3000`
   - `token`: (будет установлен автоматически после логина)

### Настройка авторизации

Для каждого запроса:
1. Перейдите на вкладку **Authorization**
2. Выберите тип: **Bearer Token**
3. В поле **Token** введите: `{{token}}`

### Примеры запросов

#### 1. Создание профиля

- **Method**: `POST`
- **URL**: `{{base_url}}/api/profiles`
- **Headers**: `Content-Type: application/json`
- **Body** (raw JSON):
```json
{
  "name": "Профиль для WhatsApp",
  "description": "Профиль для работы с WhatsApp рассылками"
}
```

#### 2. Список профилей

- **Method**: `GET`
- **URL**: `{{base_url}}/api/profiles?page=1&limit=10`

#### 3. Получение профиля

- **Method**: `GET`
- **URL**: `{{base_url}}/api/profiles/:id`
- **Params**: `id` = UUID профиля

#### 4. Обновление профиля

- **Method**: `PATCH`
- **URL**: `{{base_url}}/api/profiles/:id`
- **Body** (raw JSON):
```json
{
  "name": "Обновленное название"
}
```

#### 5. Статус профиля

- **Method**: `GET`
- **URL**: `{{base_url}}/api/profiles/:id/status`

#### 6. Удаление профиля

- **Method**: `DELETE`
- **URL**: `{{base_url}}/api/profiles/:id`

---

## 🔒 Проверка безопасности

### Тест 1: Доступ без авторизации

```bash
curl -X GET "$BASE_URL/api/profiles"
```

**Ожидаемый результат:** `401 Unauthorized`

### Тест 2: Доступ к чужому профилю

1. Создайте профиль от имени пользователя A
2. Попробуйте получить профиль от имени пользователя B

```bash
# От пользователя B
curl -X GET "$BASE_URL/api/profiles/PROFILE_ID_USER_A" \
  -H "Authorization: Bearer $TOKEN_USER_B"
```

**Ожидаемый результат:** `403 Forbidden`

### Тест 3: Валидация входных данных

```bash
# Пустое имя
curl -X POST "$BASE_URL/api/profiles" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "name": ""
  }'
```

**Ожидаемый результат:** `400 Bad Request` с описанием ошибки

### Тест 4: Невалидный UUID

```bash
curl -X GET "$BASE_URL/api/profiles/invalid-uuid" \
  -H "Authorization: Bearer $TOKEN"
```

**Ожидаемый результат:** `404 Not Found` или `400 Bad Request`

---

## 🗂️ Проверка изоляции профилей

### Тест 1: Проверка создания директории

После создания профиля проверьте, что директория создана:

```bash
# Windows PowerShell
Test-Path "./profiles/USER_ID/PROFILE_ID"

# Linux/Mac
ls -la ./profiles/USER_ID/PROFILE_ID
```

**Ожидаемый результат:** Директория должна существовать

### Тест 2: Проверка уникальности путей

1. Создайте несколько профилей
2. Проверьте, что у каждого профиля уникальный `profilePath`

**Ожидаемый результат:** Каждый профиль имеет уникальный путь

### Тест 3: Проверка каскадного удаления

1. Создайте профиль
2. Удалите пользователя (если есть такая возможность)
3. Проверьте, что профиль также удален

**Ожидаемый результат:** Профиль удален вместе с пользователем

---

## 📝 Типичные сценарии

### Сценарий 1: Полный цикл работы с профилем

```bash
# 1. Создание профиля
PROFILE_ID=$(curl -X POST "$BASE_URL/api/profiles" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"name": "Тестовый профиль"}' \
  | jq -r '.id')

# 2. Получение профиля
curl -X GET "$BASE_URL/api/profiles/$PROFILE_ID" \
  -H "Authorization: Bearer $TOKEN"

# 3. Обновление профиля
curl -X PATCH "$BASE_URL/api/profiles/$PROFILE_ID" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"name": "Обновленный профиль"}'

# 4. Получение статуса
curl -X GET "$BASE_URL/api/profiles/$PROFILE_ID/status" \
  -H "Authorization: Bearer $TOKEN"

# 5. Удаление профиля
curl -X DELETE "$BASE_URL/api/profiles/$PROFILE_ID" \
  -H "Authorization: Bearer $TOKEN"
```

### Сценарий 2: Пагинация и фильтрация

```bash
# Создайте несколько профилей, затем:

# Получить первую страницу
curl -X GET "$BASE_URL/api/profiles?page=1&limit=5" \
  -H "Authorization: Bearer $TOKEN"

# Получить вторую страницу
curl -X GET "$BASE_URL/api/profiles?page=2&limit=5" \
  -H "Authorization: Bearer $TOKEN"

# Фильтр по статусу
curl -X GET "$BASE_URL/api/profiles?status=STOPPED" \
  -H "Authorization: Bearer $TOKEN"

# Сортировка по имени
curl -X GET "$BASE_URL/api/profiles?sortBy=name&sortOrder=asc" \
  -H "Authorization: Bearer $TOKEN"
```

### Сценарий 3: Обработка ошибок

```bash
# Попытка создать профиль без имени
curl -X POST "$BASE_URL/api/profiles" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{}'

# Попытка обновить несуществующий профиль
curl -X PATCH "$BASE_URL/api/profiles/00000000-0000-0000-0000-000000000000" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"name": "Тест"}'

# Попытка удалить несуществующий профиль
curl -X DELETE "$BASE_URL/api/profiles/00000000-0000-0000-0000-000000000000" \
  -H "Authorization: Bearer $TOKEN"
```

---

## ✅ Чеклист тестирования

- [ ] Создание профиля с валидными данными
- [ ] Создание профиля без описания
- [ ] Создание профиля с невалидными данными (пустое имя)
- [ ] Получение списка профилей (базовый запрос)
- [ ] Получение списка профилей с пагинацией
- [ ] Получение списка профилей с фильтрацией по статусу
- [ ] Получение списка профилей с сортировкой
- [ ] Получение профиля по ID (существующий)
- [ ] Получение профиля по ID (несуществующий)
- [ ] Обновление профиля (все поля)
- [ ] Обновление профиля (только имя)
- [ ] Обновление профиля (только описание)
- [ ] Обновление несуществующего профиля
- [ ] Получение статуса профиля
- [ ] Удаление профиля
- [ ] Удаление несуществующего профиля
- [ ] Доступ без авторизации
- [ ] Доступ к чужому профилю
- [ ] Проверка создания директории профиля
- [ ] Проверка уникальности путей профилей

---

## 🐛 Отладка

### Проблема: 401 Unauthorized

**Решение:**
- Проверьте, что токен актуален
- Убедитесь, что токен передается в формате: `Bearer <token>`
- Проверьте, что токен не истек (access token живет 15 минут)

### Проблема: 403 Forbidden

**Решение:**
- Убедитесь, что вы пытаетесь получить доступ к своему профилю
- Проверьте, что `userId` в профиле совпадает с `userId` в токене

### Проблема: 404 Not Found

**Решение:**
- Проверьте, что профиль существует
- Убедитесь, что используете правильный UUID
- Проверьте, что профиль принадлежит текущему пользователю

### Проблема: 500 Internal Server Error

**Решение:**
- Проверьте логи сервера (`backend/logs/error.log`)
- Убедитесь, что миграция БД выполнена
- Проверьте, что директория `./profiles` доступна для записи

---

## 📊 Проверка в базе данных

### Просмотр всех профилей

```bash
cd backend
npx prisma studio
```

Откройте таблицу `profiles` и проверьте данные.

### Прямой SQL запрос (SQLite)

```bash
sqlite3 backend/prisma/prisma/dev.db "SELECT * FROM profiles;"
```

---

## 🎯 Дополнительные тесты

### Тест производительности

Создайте 100 профилей и проверьте время выполнения:

```bash
time for i in {1..100}; do
  curl -X POST "$BASE_URL/api/profiles" \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer $TOKEN" \
    -d "{\"name\": \"Профиль $i\"}"
done
```

### Тест параллельных запросов

```bash
# Создание 10 профилей параллельно
for i in {1..10}; do
  curl -X POST "$BASE_URL/api/profiles" \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer $TOKEN" \
    -d "{\"name\": \"Профиль $i\"}" &
done
wait
```

---

## 📚 Полезные ссылки

- Swagger UI: `http://localhost:3000/api-docs`
- Health Check: `http://localhost:3000/health`
- Prisma Studio: `npx prisma studio`

---

**Примечание:** Замените все `PROFILE_ID`, `USER_ID`, `TOKEN` на реальные значения при тестировании!









