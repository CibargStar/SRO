# Модуль управления профилями Chrome

Модуль для управления профилями Chrome браузера. Каждый пользователь может создавать свои изолированные профили для работы с мессенджерами и мультиаккаунтами.

## 📁 Структура модуля

```
profiles/
├── profiles.schemas.ts      # Zod схемы валидации
├── profiles.repository.ts   # Работа с БД (Prisma)
├── profiles.service.ts      # Бизнес-логика
├── profiles.controller.ts   # HTTP handlers
├── index.ts                 # Публичный API модуля
├── README.md                # Документация модуля
└── TESTING_GUIDE.md         # Гайд по тестированию
```

## 🚀 Быстрый старт

### 1. Создание миграции БД

```bash
cd backend
npx prisma migrate dev --name add_profiles_module
npx prisma generate
```

### 2. Запуск сервера

```bash
npm run dev
```

### 3. Тестирование

См. подробный гайд в [TESTING_GUIDE.md](./TESTING_GUIDE.md)

## 📡 API Endpoints

### POST `/api/profiles`
Создание нового профиля

**Требования:**
- Авторизация: ✅
- Body:
  ```json
  {
    "name": "string (1-100 символов)",
    "description": "string (до 500 символов, опционально)"
  }
  ```

### GET `/api/profiles`
Получение списка профилей пользователя

**Требования:**
- Авторизация: ✅
- Query параметры:
  - `page` (number, default: 1)
  - `limit` (number, default: 10, max: 100)
  - `status` (STOPPED | RUNNING | STARTING | STOPPING | ERROR, опционально)
  - `sortBy` (createdAt | updatedAt | name | status | lastActiveAt, default: createdAt)
  - `sortOrder` (asc | desc, default: desc)

### GET `/api/profiles/:id`
Получение профиля по ID

**Требования:**
- Авторизация: ✅
- Только владелец профиля

### PATCH `/api/profiles/:id`
Обновление профиля

**Требования:**
- Авторизация: ✅
- Только владелец профиля
- Body (все поля опциональны):
  ```json
  {
    "name": "string (1-100 символов)",
    "description": "string (до 500 символов)"
  }
  ```

### DELETE `/api/profiles/:id`
Удаление профиля

**Требования:**
- Авторизация: ✅
- Только владелец профиля

### GET `/api/profiles/:id/status`
Получение статуса профиля

**Требования:**
- Авторизация: ✅
- Только владелец профиля

## 🔒 Безопасность

- ✅ Все endpoints требуют авторизации
- ✅ Каждый пользователь видит только свои профили
- ✅ Проверка прав доступа на всех операциях
- ✅ Изоляция профилей (отдельные директории)
- ✅ Валидация всех входных данных через Zod

## 📊 Модель данных

### Profile

```typescript
{
  id: string (UUID)
  userId: string (UUID)
  name: string
  description: string | null
  profilePath: string (уникальный)
  status: ProfileStatus (STOPPED | RUNNING | STARTING | STOPPING | ERROR)
  createdAt: DateTime
  updatedAt: DateTime
  lastActiveAt: DateTime | null
}
```

### ProfileStatus

- `STOPPED` - профиль остановлен (по умолчанию)
- `RUNNING` - профиль запущен и работает
- `STARTING` - профиль запускается
- `STOPPING` - профиль останавливается
- `ERROR` - ошибка при работе профиля

## 🗂️ Изоляция профилей

Каждый профиль создается в отдельной директории:
```
./profiles/{userId}/{profileId}/
```

Директория создается автоматически при создании профиля.

## 🔗 Связи

- `User -> Profile` (один-ко-многим, каскадное удаление)
- При удалении пользователя все его профили удаляются автоматически

## 📝 Примеры использования

### Создание профиля

```typescript
const profile = await profilesService.createProfile(userId, {
  name: "Профиль для WhatsApp",
  description: "Профиль для работы с WhatsApp рассылками"
});
```

### Получение списка профилей

```typescript
const result = await profilesService.listProfiles(userId, {
  page: 1,
  limit: 10,
  status: 'STOPPED',
  sortBy: 'createdAt',
  sortOrder: 'desc'
});
```

### Обновление профиля

```typescript
const updated = await profilesService.updateProfile(profileId, userId, {
  name: "Обновленное название"
});
```

## 🐛 Отладка

### Логи

Все операции логируются через Winston:
- `backend/logs/combined.log` - все логи
- `backend/logs/error.log` - только ошибки

### Prisma Studio

Для просмотра данных в БД:
```bash
npx prisma studio
```

## 📚 Дополнительная документация

- [Гайд по тестированию](./TESTING_GUIDE.md)
- [Swagger UI](http://localhost:3000/api-docs)

## ⚠️ Важные замечания

1. **Миграция БД обязательна** - перед использованием модуля необходимо выполнить миграцию
2. **Директория профилей** - убедитесь, что приложение имеет права на запись в директорию `./profiles`
3. **Токены** - access token живет 15 минут, при истечении необходимо обновить через `/api/auth/refresh`

## 🔮 Будущие улучшения

- [ ] Интеграция с Puppeteer для управления Chrome
- [ ] Управление ресурсами (CPU, память, сеть)
- [ ] Мониторинг активности профилей
- [ ] Лимиты профилей для пользователей
- [ ] Фоновые процессы для работы Chrome









