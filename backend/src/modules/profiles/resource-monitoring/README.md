# Модуль мониторинга ресурсов профилей

Модуль для мониторинга использования ресурсов Chrome процессов. Отслеживает CPU, память и сетевую активность.

## 📁 Структура модуля

```
resource-monitoring/
├── process-resources.manager.ts  # Низкоуровневое получение статистики
├── resource-monitor.service.ts   # Высокоуровневый сервис
├── index.ts                      # Публичный API
└── README.md                     # Документация
```

## 🎯 Назначение

Модуль обеспечивает:

1. **Мониторинг CPU** - отслеживание использования процессора
2. **Мониторинг памяти** - отслеживание использования оперативной памяти
3. **Кэширование статистики** - для быстрого доступа
4. **Кросс-платформенность** - поддержка Windows, Linux, macOS

## 🔧 Использование

### Базовое использование

```typescript
import { ResourceMonitorService } from './resource-monitoring';
import { ChromeProcessService } from '../chrome-process';

const chromeService = new ChromeProcessService(isolationService);
const monitorService = new ResourceMonitorService(chromeService);

// Получение статистики для профиля
const stats = await monitorService.getProfileResourceStats(profileId);

// Получение статистики для всех профилей
const allStats = await monitorService.getAllProfilesResourceStats();
```

## 📡 API

### ResourceMonitorService

#### `getProfileResourceStats(profileId)`
Получает статистику ресурсов для профиля.

```typescript
const stats = await monitorService.getProfileResourceStats(profileId);
// {
//   profileId: string,
//   pid: number,
//   cpuUsage: number, // 0-100
//   memoryUsage: number, // MB
//   memoryUsagePercent: number, // 0-100
//   timestamp: Date
// }
```

#### `getAllProfilesResourceStats()`
Получает статистику для всех запущенных профилей.

```typescript
const allStats = await monitorService.getAllProfilesResourceStats();
// ProcessResourceStats[]
```

### ProcessResourcesManager

Низкоуровневый менеджер для работы с системными процессами.

#### `getProcessStats(pid, profileId)`
Получает статистику процесса по PID.

```typescript
const stats = await manager.getProcessStats(pid, profileId);
```

## ⚙️ Поддерживаемые платформы

### Windows
- Использует `wmic` для получения информации о процессах
- Получает `WorkingSetSize` (память) и `PercentProcessorTime` (CPU)

### Linux / macOS
- Использует `ps` для получения информации о процессах
- Получает `%cpu` и `rss` (память)
- Использует `/proc/meminfo` (Linux) или `sysctl` (macOS) для общего объема памяти

## 🔒 Безопасность

1. **Валидация PID** - проверка существования процесса
2. **Обработка ошибок** - безопасная обработка всех ошибок
3. **Кэширование** - для снижения нагрузки на систему

## ⚠️ Ограничения

1. **Точность мониторинга** - зависит от возможностей операционной системы
2. **Производительность** - получение статистики может быть медленным на некоторых системах
3. **Сетевая активность** - пока не реализована (для будущих версий)

## 🔮 Будущие улучшения

- [ ] Мониторинг сетевой активности
- [ ] Периодический сбор статистики
- [ ] Агрегация статистики за период
- [ ] Уведомления при превышении лимитов
- [ ] Графики использования ресурсов









