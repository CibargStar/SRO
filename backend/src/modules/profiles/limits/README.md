# Модуль управления лимитами профилей

Модуль для управления лимитами профилей пользователей. Позволяет ROOT пользователю настраивать максимальное количество профилей и лимиты ресурсов для каждого пользователя.

## 📁 Структура модуля

```
limits/
├── limits.repository.ts  # Работа с БД (Prisma)
├── limits.service.ts      # Бизнес-логика
├── limits.controller.ts   # HTTP handlers
├── limits.schemas.ts      # Zod валидация
├── index.ts               # Публичный API
└── README.md              # Документация
```

## 🎯 Назначение

Модуль обеспечивает:

1. **Лимиты профилей** - максимальное количество профилей для каждого пользователя
2. **Лимиты ресурсов** - максимальное использование CPU, памяти, сети на профиль (для будущего использования)
3. **Управление лимитами** - ROOT может настраивать лимиты для любого пользователя
4. **Проверка лимитов** - автоматическая проверка при создании профилей

## 🔧 Использование

### Базовое использование

```typescript
import { ProfileLimitsService } from './limits';
import { ProfilesRepository } from '../profiles.repository';

const limitsRepository = new ProfileLimitsRepository(prisma);
const profilesRepository = new ProfilesRepository(prisma);
const limitsService = new ProfileLimitsService(limitsRepository, profilesRepository);

// Получение лимитов пользователя
const limits = await limitsService.getUserLimits(userId);

// Проверка возможности создания профиля
await limitsService.canCreateProfile(userId);

// Установка лимитов (для ROOT)
await limitsService.setUserLimits(userId, {
  maxProfiles: 20,
  maxCpuPerProfile: 0.5,
  maxMemoryPerProfile: 512,
}, rootUserId);
```

## 📡 API

### ProfileLimitsService

#### `getUserLimits(userId)`
Получает лимиты пользователя. Если лимиты не установлены, возвращает значения по умолчанию.

```typescript
const limits = await limitsService.getUserLimits(userId);
// { maxProfiles: 10, maxCpuPerProfile: null, ... }
```

#### `canCreateProfile(userId)`
Проверяет, можно ли создать новый профиль (не превышен ли лимит).

```typescript
try {
  await limitsService.canCreateProfile(userId);
  // Можно создавать профиль
} catch (error) {
  // Лимит превышен
}
```

#### `setUserLimits(userId, data, updatedBy)`
Устанавливает лимиты для пользователя (только для ROOT).

```typescript
const limits = await limitsService.setUserLimits(userId, {
  maxProfiles: 20,
  maxCpuPerProfile: 0.5,
  maxMemoryPerProfile: 512,
}, rootUserId);
```

## 🔒 Безопасность

1. **Проверка прав доступа** - только ROOT может управлять лимитами других пользователей
2. **Валидация данных** - все входные данные валидируются через Zod
3. **Проверка лимитов** - нельзя установить лимит меньше текущего количества профилей

## 📊 Модель данных

### UserProfileLimits

```typescript
{
  id: string (UUID)
  userId: string (UUID, unique)
  maxProfiles: number (default: 10)
  maxCpuPerProfile: number | null (0-1, опционально)
  maxMemoryPerProfile: number | null (MB, опционально)
  maxNetworkPerProfile: number | null (KB/s, опционально)
  createdAt: DateTime
  updatedAt: DateTime
  updatedBy: string | null (ID ROOT пользователя)
}
```

## 🔗 Связи

- `User -> UserProfileLimits` (один-к-одному, каскадное удаление)
- При удалении пользователя его лимиты удаляются автоматически

## 📝 Примеры использования

### Проверка лимитов при создании профиля

```typescript
// В ProfilesService.createProfile()
if (this.limitsService) {
  await this.limitsService.canCreateProfile(userId);
}
```

### Установка лимитов для пользователя

```typescript
// ROOT устанавливает лимиты
await limitsService.setUserLimits(userId, {
  maxProfiles: 20,
  maxCpuPerProfile: 0.5,
  maxMemoryPerProfile: 512,
}, rootUserId);
```

## ⚠️ Важные замечания

1. **Лимиты по умолчанию** - если лимиты не установлены, используется `maxProfiles: 10`
2. **Проверка при установке** - нельзя установить `maxProfiles` меньше текущего количества профилей
3. **Лимиты ресурсов** - `maxCpuPerProfile`, `maxMemoryPerProfile`, `maxNetworkPerProfile` пока не используются, но сохранены для будущего использования

## 🔮 Будущие улучшения

- [ ] Применение лимитов ресурсов (CPU, память) при запуске Chrome
- [ ] Мониторинг использования ресурсов и автоматическое ограничение
- [ ] Уведомления при приближении к лимитам
- [ ] Автоматическое масштабирование лимитов









