/**
 * Маршруты приложения
 * 
 * Здесь определяются все API endpoints приложения.
 * Используйте Express Router для организации маршрутов по модулям.
 * 
 * @example
 * ```typescript
 * import { Router } from 'express';
 * const router = Router();
 * router.get('/users', getUsersHandler);
 * export default router;
 * ```
 */

