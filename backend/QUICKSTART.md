# Быстрый старт - Разработка

## Шаг 1: Установка зависимостей

```bash
cd backend
npm install
```

## Шаг 2: Настройка окружения

Создайте файл `.env` в папке `backend/` на основе `.env.example`:

```bash
# Скопируйте пример
cp .env.example .env
```

Или создайте вручную с минимальными значениями:

```env
NODE_ENV=development
PORT=3000
DATABASE_URL=file:./prisma/dev.db
LOG_LEVEL=debug

# Frontend URL для CORS
FRONTEND_URL=http://localhost:5173

# Авторизация - Root пользователь
ROOT_EMAIL=admin@example.com
ROOT_PASSWORD=AdminPassword123!

# JWT секреты (для development можно использовать простые, но лучше сгенерировать)
JWT_ACCESS_SECRET=dev-access-secret-min-32-chars-long-change-in-production
JWT_REFRESH_SECRET=dev-refresh-secret-min-32-chars-long-change-in-production

# Время жизни токенов
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
```

**Важно:** Для production сгенерируйте криптографически случайные секреты:
```bash
# Linux/Mac
openssl rand -base64 32

# Windows PowerShell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
```

## Шаг 3: Генерация Prisma Client и миграции

```bash
# Генерация Prisma Client
npm run prisma:generate

# Применение миграций (создаст БД если её нет)
npm run prisma:migrate
```

## Шаг 4: Запуск в режиме разработки

```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

## Шаг 5: Просмотр Swagger документации

Откройте в браузере:
- **Swagger UI:** http://localhost:3000/api-docs
- **Health Check:** http://localhost:3000/health

## Шаг 6: Запуск тестов

```bash
# Все тесты
npm test

# Тесты в watch режиме
npm run test:watch

# Тесты с покрытием
npm run test:coverage
```

## Полезные команды

```bash
# Проверка типов TypeScript
npm run type-check

# Линтинг
npm run lint

# Исправление ошибок линтинга
npm run lint:fix

# Prisma Studio (GUI для БД)
npm run prisma:studio
```

## Структура API

### Auth эндпоинты (без авторизации):
- `POST /api/auth/login` - вход в систему
- `POST /api/auth/refresh` - обновление токенов
- `POST /api/auth/logout` - выход из системы

### Users эндпоинты (требуют авторизации):
- `GET /api/users/me` - данные текущего пользователя (любой авторизованный)
- `GET /api/users` - список пользователей (только ROOT)
- `POST /api/users` - создание пользователя (только ROOT)
- `PATCH /api/users/:id` - обновление пользователя (только ROOT)

## Тестирование API через Swagger

1. Откройте http://localhost:3000/api-docs
2. Нажмите кнопку **"Authorize"** (🔓) в правом верхнем углу
3. Введите Bearer токен (получите через POST /api/auth/login)
4. Теперь можете тестировать защищенные эндпоинты

## Первый вход

После запуска сервера ROOT пользователь создается автоматически из переменных окружения:
- Email: значение из `ROOT_EMAIL`
- Password: значение из `ROOT_PASSWORD`

Используйте эти данные для входа через POST /api/auth/login

